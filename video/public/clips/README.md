# Aura video clip inputs

Place locally captured or Hedra-generated footage here. Video files are ignored by Git so large media and accidental private information are not committed.

Recommended names:

- `hedra-intro.mp4` — optional cinematic Aura opening;
- `hedra-problem.mp4` — optional fragmented-intent visualization;
- `console-demo.mp4` — genuine browser recording of the deterministic Aura console;
- `hedra-impact.mp4` — optional coordinated-settlement visualization.

The `AuraDemo` composition renders polished programmatic fallbacks when optional Hedra files are absent. The console segment intentionally renders a placeholder until genuine browser footage is supplied.
